var playlist = {names: "song titles"};

function updatePlaylist('playlist', 'names', 'song title') {
  return playlist.assign ({}, 'playist')
}

function removeFromPlaylist('playlist', 'name')
delete playlist.names;
{return playlist}
